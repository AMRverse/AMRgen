% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{sirscan_codes}
\alias{sirscan_codes}
\title{SIRscan antibiotic code mapping}
\format{
\code{sirscan_codes} A data frame with 11 rows and 2 columns:

Columns include:
\itemize{
\item \code{sirscan_code}: The antibiotic code as it appears in SIRscan export files.
\item \code{ab_name}: Antibiotic name string passed to \code{\link[AMR:as.ab]{AMR::as.ab()}} for translation.
}
}
\usage{
sirscan_codes
}
\description{
Reference table mapping Bio-Rad SIRscan antibiotic codes to antibiotic names
recognised by \code{\link[AMR:as.ab]{AMR::as.ab()}}. Used as the default lookup in
\code{\link[=import_sirscan_ast]{import_sirscan_ast()}} to translate codes that the AMR package does not
recognise directly. Users can supply their own table via the \code{sirscan_codes}
argument if their SIRscan configuration uses different code meanings.
}
\details{
Confirmed codes (verified from Bio-Rad catalogue or collaborator communication):
\code{CF30} (cephalothin 30 µg disk), \code{MA} (cefamandole), \code{SSS} (sulfonamides),
\code{SXT25} (trimethoprim/sulfamethoxazole 25 µg disk).

Codes mapped by French CASFM/SIRscan convention, not yet confirmed in official
Bio-Rad documentation: \code{S} (streptomycin), \code{K} (kanamycin),
\code{C} (chloramphenicol), \code{NALI} (nalidixic acid), \code{AZ} (azithromycin),
\code{MM} (minocycline), \code{ERM} (erythromycin).

Codes with no confirmed mapping (\code{CAN}, \code{ECM}, \code{ICM}) are absent from this
table; they will return \code{NA} from \code{\link[AMR:as.ab]{AMR::as.ab()}} with a warning. Update this
table or supply your own once the correct mappings are known.
}
\keyword{datasets}
