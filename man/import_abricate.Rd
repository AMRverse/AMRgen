% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_abricate.R
\name{import_abricate}
\alias{import_abricate}
\title{Import and Process Abricate Results}
\usage{
import_abricate(input_table, sample_col = "Name", database = resfinder)
}
\arguments{
\item{input_table}{A character string specifying a dataframe or path to the Abricate results table.}

\item{sample_col}{A character string specifying the column that identifies samples in the dataset (default \code{"Name"}).}

\item{database}{A dataframe or tibble containing a reference table mapping drug agents (\code{drug_agent}) to standardised drug classes (\code{drug_class}). Defaults to \code{resfinder}, which is provided internally by the package. Currently only the resfinder database is supported.}
}
\value{
A tibble containing the processed AMR elements, with harmonised gene names, mapped drug agents, and drug classes which can be used for other functions of the ARMgen package.
}
\description{
This function imports and processes Abricate results (e.g., using the ResFinder database), extracting antimicrobial resistance (AMR) elements and mapping them to standardised antibiotic names and drug classes. The function also standardises sample names by replacing dashes with underscores to ensure compatibility with other AMRverse functions.
}
\details{
The function performs the following steps:
\itemize{
\item Reads the Abricate output table via the internal \code{process_input} function.
\item Standardises the sample column name and format (replacing hyphens with underscores).
\item Assigns standardised column names for genes, markers, and variation types (defaulting to "Gene presence detected").
\item Splits multiple resistance annotations (separated by semicolons) into separate rows.
\item Maps drug agents to standardised drug class names using the provided \code{database}.
\item Converts drug agent names to the \code{"ab"} class from the AMR package.
}
}
\examples{
\dontrun{
# Assuming 'abricate_raw' is a loaded dataframe of Abricate results
abricate_processed <- import_abricate(abricate_raw, sample_col = "Name")
head(abricate_processed)
}
}
